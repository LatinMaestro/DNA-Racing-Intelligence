# Phase 7 Lifecycle Economic Forms

## Status

The authenticated Lifecycle route renders this accessible evidence shell, but
every field and submit control remains disabled while owner-scoped write
persistence and strict form parsing are unavailable.

## Contract

- Present separate semantic forms for confirmed core sale evidence, completed
  non-Genesis burn evidence and actual post-burn BGC credit.
- Keep missing acquisition cost explicit and realised gain unavailable rather
  than fabricated.
- Omit Genesis from burn choices and state the permanent prohibition.
- Never predict a burn-credit amount; accept only actual BGC linked to durable
  burn evidence.
- Distinguish actual post-burn BGC from historical Race Merge BGC rows, which
  retain performance evidence but have zero race economics.
- Label every control and expose the capability state in text.
- Never request wallet credentials, private keys, seed phrases or signing
  material and never initiate a sale, burn, wallet, game or ownership action.
- Keep every fieldset and submit button disabled until the owner-scoped
  forced-RLS write repository is connected and validated.

## Remaining evidence

- Add strict `FormData` parsing, server-generated durable IDs, multi-fee sale
  controls and action-state feedback.
- Exercise record, replay, conflict, Genesis rejection, missing-cost-basis and
  multiple-credit review paths against persistent Preview storage.
