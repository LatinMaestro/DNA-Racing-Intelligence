name: TEMPORARY elite opponent vault/esports analysis - DO NOT MERGE

on:
  push:
    branches:
      - temporary/r2-evidence-analysis-do-not-merge-2026-09-05
    paths:
      - tests/temporary-elite-opponent-vault-esports-analysis-do-not-merge.test.ts
      - .github/workflows/temporary-elite-opponent-vault-esports-analysis-do-not-merge.yml

permissions:
  contents: read

jobs:
  analyze:
    if: github.ref == 'refs/heads/temporary/r2-evidence-analysis-do-not-merge-2026-09-05'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      DNA_ELITE_OPPONENT_ANALYSIS: '1'
      DNA_OPEN_LAB_API_KEY_1: ${{ secrets.DNA_OPEN_LAB_API_KEY_1 }}
    steps:
      - name: Guard temporary branch
        run: test "$GITHUB_REF" = "refs/heads/temporary/r2-evidence-analysis-do-not-merge-2026-09-05"
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Pull public vault and esports evidence
        run: npx vitest run tests/temporary-elite-opponent-vault-esports-analysis-do-not-merge.test.ts --reporter=verbose
      - name: Verify no working tree mutation
        run: git diff --exit-code && git diff --cached --exit-code
      - uses: actions/upload-artifact@v4
        with:
          name: TEMPORARY-DO-NOT-MERGE-elite-opponent-${{ github.run_id }}
          path: artifacts/temporary-elite-opponent/
          retention-days: 1
