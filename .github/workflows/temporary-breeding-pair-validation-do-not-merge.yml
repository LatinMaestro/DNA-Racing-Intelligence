name: TEMPORARY breeding pair validation - DO NOT MERGE

# TEMPORARY READ-ONLY DNA API WORKFLOW. DO NOT MERGE INTO main.
on:
  push:
    branches:
      - temporary/r2-evidence-analysis-do-not-merge-2026-09-05
    paths:
      - tests/temporary-breeding-pair-validation-do-not-merge.test.ts
      - .github/workflows/temporary-breeding-pair-validation-do-not-merge.yml
permissions:
  contents: read
jobs:
  validate-pairs:
    name: TEMPORARY ONLY - breeding pair checks - DO NOT MERGE
    if: github.ref == 'refs/heads/temporary/r2-evidence-analysis-do-not-merge-2026-09-05'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      TEMP_BREEDING_PAIR_VALIDATION: '1'
      DNA_OPEN_LAB_API_KEY_1: ${{ secrets.DNA_OPEN_LAB_API_KEY_1 }}
    steps:
      - name: Guard branch
        shell: bash
        run: |
          set -euo pipefail
          [[ "${GITHUB_REF}" == "refs/heads/temporary/r2-evidence-analysis-do-not-merge-2026-09-05" ]]
      - name: Checkout temporary branch only
        uses: actions/checkout@v4
        with:
          ref: temporary/r2-evidence-analysis-do-not-merge-2026-09-05
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install existing dependencies without scripts
        run: npm ci --ignore-scripts
      - name: Run read-only pair checks
        run: npx vitest run tests/temporary-breeding-pair-validation-do-not-merge.test.ts --reporter=dot
      - name: Prove tracked files unchanged
        run: git diff --exit-code && git diff --cached --exit-code
      - name: Upload sanitized pair results for one day
        uses: actions/upload-artifact@v4
        with:
          name: TEMPORARY-DO-NOT-MERGE-breeding-pair-validation-${{ github.run_id }}
          path: artifacts/temporary-breeding-pair-validation.json
          retention-days: 1
          if-no-files-found: error
