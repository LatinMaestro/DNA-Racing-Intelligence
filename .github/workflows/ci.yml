name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
      - name: Scan tracked files for common secrets
        shell: bash
        run: |
          if git grep -nIE '(-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9_]{36,255}|sk_(live|test|proj)_[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{20,})' -- . ':!package-lock.json'; then
            echo "A tracked file contains a credential-like value."
            exit 1
          fi
      - run: npm run format
      - run: npm run lint
      - run: npm run typecheck
      - name: Verify generated Cloudflare Worker bindings
        run: npm run worker:types:check
      - name: Validate Cloudflare Worker bundle without deployment
        run: npm run worker:dry-run
      - run: npm test
      - run: npm run build
        env:
          VERCEL: "0"
          ALLOW_PRODUCTION_DEPLOYMENT: "false"

  migration:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: dna_racing_ci
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d dna_racing_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      PGHOST: localhost
      PGPORT: 5432
      PGDATABASE: dna_racing_ci
      PGUSER: postgres
      PGPASSWORD: postgres
    steps:
      - uses: actions/checkout@v4
      - name: Apply data-foundation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0001_data_foundation.up.sql
      - name: Run data-foundation smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0001_data_foundation.smoke.sql
      - name: Apply dataset-acceptance migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0002_dataset_acceptance.up.sql
      - name: Run dataset-acceptance smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0002_dataset_acceptance.smoke.sql
      - name: Apply normalized Race Merge materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0003_race_materialization.up.sql
      - name: Run normalized Race Merge materialization smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0003_race_materialization.smoke.sql
      - name: Apply star-profile materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0004_star_profile_materialization.up.sql
      - name: Run star-profile materialization smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0004_star_profile_materialization.smoke.sql
      - name: Apply star-observation reconciliation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0005_star_observation_reconciliation.up.sql
      - name: Run star-observation reconciliation smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0005_star_observation_reconciliation.smoke.sql
      - name: Apply lineage graph migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0006_lineage_graph.up.sql
      - name: Run lineage graph smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0006_lineage_graph.smoke.sql
      - name: Apply Core Details materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0007_core_materialization.up.sql
      - name: Run Core Details materialization smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0007_core_materialization.smoke.sql
      - name: Apply current-snapshot materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0008_snapshot_materialization.up.sql
      - name: Run current-snapshot materialization smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0008_snapshot_materialization.smoke.sql
      - name: Apply race-economic ledger migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0009_race_economic_ledger.up.sql
      - name: Run race-economic ledger smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0009_race_economic_ledger.smoke.sql
      - name: Apply import-operation reservation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0010_import_operation_reservation.up.sql
      - name: Run import-operation reservation smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0010_import_operation_reservation.smoke.sql
      - name: Prepare direct runtime-login fixture
        shell: bash
        run: |
          psql --set ON_ERROR_STOP=1 <<'SQL'
          ALTER ROLE dna_app_runtime PASSWORD 'runtime-ci-only';
          INSERT INTO dna.app_owner (id, clerk_user_id)
          VALUES
            ('11111111-1111-4111-8111-111111111111', 'direct-runtime-owner-1'),
            ('22222222-2222-4222-8222-222222222222', 'direct-runtime-owner-2');
          INSERT INTO dna.import_operation_reservation (
            id,
            owner_id,
            operation_kind,
            idempotency_key,
            request_fingerprint_sha256,
            requested_at
          )
          VALUES
            (
              '11111111-1111-4111-8111-111111111110',
              '11111111-1111-4111-8111-111111111111',
              'upload_batch',
              'direct-runtime-owner-1-fixture',
              repeat('a', 64)::character(64),
              '2026-08-13T00:00:00.000Z'
            ),
            (
              '22222222-2222-4222-8222-222222222220',
              '22222222-2222-4222-8222-222222222222',
              'upload_batch',
              'direct-runtime-owner-2-fixture',
              repeat('b', 64)::character(64),
              '2026-08-13T00:00:00.000Z'
            );
          SQL
      - name: Prove direct least-privilege runtime session
        env:
          PGUSER: dna_app_runtime
          PGPASSWORD: runtime-ci-only
        run: psql --set ON_ERROR_STOP=1 --file database/preflight/runtime_session.sql
      - name: Remove direct runtime-login fixture
        shell: bash
        run: |
          psql --set ON_ERROR_STOP=1 <<'SQL'
          DELETE FROM dna.import_operation_reservation
          WHERE owner_id IN (
            '11111111-1111-4111-8111-111111111111',
            '22222222-2222-4222-8222-222222222222'
          );
          DELETE FROM dna.app_owner
          WHERE id IN (
            '11111111-1111-4111-8111-111111111111',
            '22222222-2222-4222-8222-222222222222'
          );
          ALTER ROLE dna_app_runtime PASSWORD NULL;
          SQL
      - name: Apply private-upload reservation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0011_private_upload_reservation.up.sql
      - name: Run private-upload reservation smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0011_private_upload_reservation.smoke.sql
      - name: Apply retired Current Vault upload migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0012_retire_current_vault_upload.up.sql
      - name: Run retired Current Vault upload smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0012_retire_current_vault_upload.smoke.sql
      - name: Apply owner Vault registry migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0013_owner_vault_registry.up.sql
      - name: Run owner Vault registry smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0013_owner_vault_registry.smoke.sql
      - name: Apply owner Vault catalogue read migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0014_owner_vault_catalogue_read.up.sql
      - name: Run owner Vault catalogue read smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0014_owner_vault_catalogue_read.smoke.sql
      - name: Apply Core performance profile migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0015_core_performance_profiles.up.sql
      - name: Run Core performance profile smoke test
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0015_core_performance_profiles.smoke.sql
      - name: Reverse Core performance profile migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0015_core_performance_profiles.down.sql
      - name: Verify Core performance profile removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'elapsed_seconds_to_milliseconds',
          'refresh_core_performance_profiles',
          'list_core_performance_profiles'
          )
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname = 'core_performance_profile'
          ) THEN RAISE EXCEPTION 'Core performance profile objects still exist';
          END IF; END \$\$;"
      - name: Reverse owner Vault catalogue read migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0014_owner_vault_catalogue_read.down.sql
      - name: Verify owner Vault catalogue read removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'search_owner_vault_catalogue'
          ) THEN RAISE EXCEPTION 'owner Vault catalogue search still exists';
          END IF; END \$\$;"
      - name: Reverse owner Vault registry migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0013_owner_vault_registry.down.sql
      - name: Verify owner Vault registry removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'set_owner_vault_core'
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'owner_vault_core',
          'owner_vault_mutation_receipt'
          )
          ) THEN RAISE EXCEPTION 'owner Vault registry objects still exist';
          END IF; END \$\$;"
      - name: Reverse retired Current Vault upload migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0012_retire_current_vault_upload.down.sql
      - name: Verify retired Current Vault upload reversal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'enforce_import_upload_batch_file_limit'
          ) OR NOT EXISTS (
          SELECT 1 FROM pg_constraint constraint_record
          WHERE constraint_record.conrelid = 'dna.import_upload_file'::regclass
          AND constraint_record.conname = 'import_upload_file_source_family_check'
          AND pg_get_constraintdef(constraint_record.oid) LIKE '%current_vault%'
          ) THEN RAISE EXCEPTION 'retired Current Vault upload migration did not reverse cleanly';
          END IF; END \$\$;"
      - name: Reverse private-upload reservation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0011_private_upload_reservation.down.sql
      - name: Verify private-upload reservation removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'reserve_import_upload_batch',
          'mark_import_upload_targets_ready',
          'mark_import_upload_reservation_failed'
          )
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'import_upload_batch',
          'import_upload_file'
          )
          ) THEN RAISE EXCEPTION 'private-upload reservation objects still exist';
          END IF; END \$\$;"
      - name: Reverse import-operation reservation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0010_import_operation_reservation.down.sql
      - name: Verify import-operation reservation removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'reserve_import_operation'
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname = 'import_operation_reservation'
          ) OR EXISTS (
          SELECT 1 FROM pg_catalog.pg_roles
          WHERE rolname = 'dna_app_runtime'
          ) THEN RAISE EXCEPTION 'import-operation reservation objects still exist';
          END IF; END \$\$;"
      - name: Reverse race-economic ledger migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0009_race_economic_ledger.down.sql
      - name: Verify race-economic ledger removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'exact_decimal_to_atomic',
          'race_economic_natural_key',
          'materialize_owned_race_economics',
          'record_daily_usd_rate',
          'refresh_race_usd_valuations',
          'rollback_active_dataset_pre_economics'
          )
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'daily_usd_rate',
          'race_economic_contribution',
          'economic_transaction_usd_valuation',
          'current_race_economic_usd',
          'race_economic_usd_coverage'
          )
          ) OR EXISTS (
          SELECT 1 FROM information_schema.columns
          WHERE table_schema = 'dna'
          AND (
          (table_name = 'normalized_race_staged_fact'
          AND column_name IN (
          'race_asset',
          'entry_fee_amount',
          'gross_payout_amount',
          'payout_mechanism_source_value',
          'race_tags_source_value'
          ))
          OR (table_name = 'race_entry_source'
          AND column_name = 'raw_race_tags')
          OR (table_name = 'race_event'
          AND column_name IN (
          'payout_mechanism_source_value',
          'race_tags_source_value'
          ))
          )
          ) THEN RAISE EXCEPTION 'race-economic ledger objects still exist';
          END IF; END \$\$;"
      - name: Reverse current-snapshot materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0008_snapshot_materialization.down.sql
      - name: Verify current-snapshot materialization removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'accept_staged_vault_dataset',
          'accept_staged_arena_dataset',
          'rollback_active_dataset_pre_snapshot'
          )
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'normalized_vault_staged_fact',
          'normalized_arena_staged_fact',
          'vault_snapshot_entry',
          'arena_snapshot_entry',
          'current_vault_snapshot_entry',
          'current_arena_snapshot_entry'
          )
          ) THEN RAISE EXCEPTION 'current-snapshot materialization objects still exist';
          END IF; END \$\$;"
      - name: Reverse Core Details materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0007_core_materialization.down.sql
      - name: Verify Core Details materialization removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'accept_staged_core_dataset',
          'evaluate_family_pair_graph',
          'refresh_core_lineage_unfiltered',
          'rollback_active_dataset_pre_core'
          )
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'normalized_core_staged_fact',
          'core_parent_import_provenance',
          'active_core_details'
          )
          ) OR EXISTS (
          SELECT 1 FROM information_schema.columns
          WHERE table_schema = 'dna'
          AND table_name = 'core_parent'
          AND column_name = 'active_in_core_details'
          ) THEN RAISE EXCEPTION 'Core Details materialization objects still exist';
          END IF; END \$\$;"
      - name: Reverse lineage graph migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0006_lineage_graph.down.sql
      - name: Verify lineage graph removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN ('refresh_core_lineage', 'evaluate_family_pair')
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'core_lineage_reachability',
          'core_lineage_validation_issue'
          )
          ) THEN RAISE EXCEPTION 'lineage graph objects still exist';
          END IF; END \$\$;"
      - name: Reverse star-observation reconciliation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0005_star_observation_reconciliation.down.sql
      - name: Verify star-observation reconciliation removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'reconcile_manual_star_observations'
          ) OR EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'manual_star_observation_reconcile_queue',
          'race_event_reconcile_candidate'
          )
          ) THEN RAISE EXCEPTION 'star-observation reconciliation objects still exist';
          END IF; END \$\$;"
      - name: Reverse star-profile materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0004_star_profile_materialization.down.sql
      - name: Verify star-profile materialization removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname = 'core_star_profile'
          ) OR EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname = 'refresh_star_profiles'
          ) OR EXISTS (
          SELECT 1 FROM information_schema.columns
          WHERE table_schema = 'dna'
          AND table_name = 'event_star_validation'
          AND column_name = 'gold_source_core_ids'
          ) THEN RAISE EXCEPTION 'star-profile materialization objects still exist';
          END IF; END \$\$;"
      - name: Reverse normalized Race Merge materialization migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0003_race_materialization.down.sql
      - name: Verify normalized Race Merge materialization removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname = 'normalized_race_staged_fact'
          ) OR EXISTS (
          SELECT 1 FROM information_schema.columns
          WHERE table_schema = 'dna'
          AND table_name IN ('race_event', 'race_entry')
          AND column_name = 'active_in_dataset'
          ) OR EXISTS (
          SELECT 1 FROM pg_proc proc
          JOIN pg_namespace namespace ON namespace.oid = proc.pronamespace
          WHERE namespace.nspname = 'dna'
          AND proc.proname IN (
          'accept_staged_race_dataset',
          'rollback_active_dataset_ledger'
          )
          ) THEN RAISE EXCEPTION 'race materialization objects still exist';
          END IF; END \$\$;"
      - name: Reverse dataset-acceptance migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0002_dataset_acceptance.down.sql
      - name: Verify dataset-acceptance removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_class relation
          JOIN pg_namespace namespace ON namespace.oid = relation.relnamespace
          WHERE namespace.nspname = 'dna'
          AND relation.relname IN (
          'dataset_stream', 'dataset_staged_record',
          'dataset_version_record', 'dataset_record_contribution',
          'active_dataset_record'
          )
          ) THEN RAISE EXCEPTION 'dataset acceptance objects still exist';
          END IF; END \$\$;"
      - name: Reverse data-foundation migration
        run: psql --set ON_ERROR_STOP=1 --file database/migrations/0001_data_foundation.down.sql
      - name: Verify schema removal
        run: >-
          psql --set ON_ERROR_STOP=1 --command
          "DO \$\$ BEGIN IF EXISTS (
          SELECT 1 FROM pg_namespace WHERE nspname = 'dna'
          ) THEN RAISE EXCEPTION 'dna schema still exists'; END IF; END \$\$;"

  commission_preview_worker:
    name: Exact-head Preview Worker commission
    needs: [validate, migration]
    if: github.event_name == 'pull_request' && github.head_ref == 'codex/commission-worker-after-ci'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      AUTHORIZED_CLERK_USER_ID: ${{ secrets.AUTHORIZED_CLERK_USER_ID }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DNA_DATABASE_OWNER_ID: ${{ secrets.DNA_DATABASE_OWNER_ID }}
      DNA_R2_ACCESS_KEY_ID: ${{ secrets.DNA_R2_ACCESS_KEY_ID }}
      DNA_R2_SECRET_ACCESS_KEY: ${{ secrets.DNA_R2_SECRET_ACCESS_KEY }}
      WORKER_SECRETS_FILE: ${{ runner.temp }}/dna-racing-preview-worker-secrets.json
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts
      - name: Validate runtime secret contract
        run: |
          node --input-type=module <<'NODE'
          const required = [
            "AUTHORIZED_CLERK_USER_ID",
            "CLOUDFLARE_ACCOUNT_ID",
            "CLOUDFLARE_API_TOKEN",
            "DATABASE_URL",
            "DNA_DATABASE_OWNER_ID",
            "DNA_R2_ACCESS_KEY_ID",
            "DNA_R2_SECRET_ACCESS_KEY",
          ];
          for (const name of required) {
            const value = process.env[name];
            if (!value || value.trim() !== value || value.length > 4096) {
              throw new Error(name + " is missing, padded, or unexpectedly large");
            }
          }
          if (!process.env.AUTHORIZED_CLERK_USER_ID.startsWith("user_")) {
            throw new Error("AUTHORIZED_CLERK_USER_ID must be a Clerk user ID");
          }
          if (process.env.DNA_DATABASE_OWNER_ID !== process.env.EXPECTED_DATABASE_OWNER_ID) {
            throw new Error("DNA_DATABASE_OWNER_ID does not match the commissioned Preview owner");
          }
          if (!/^[a-f0-9]{32}$/.test(process.env.CLOUDFLARE_ACCOUNT_ID)) {
            throw new Error("CLOUDFLARE_ACCOUNT_ID has an unexpected shape");
          }
          const databaseUrl = new URL(process.env.DATABASE_URL);
          if (!["postgres:", "postgresql:"].includes(databaseUrl.protocol)) {
            throw new Error("DATABASE_URL is not PostgreSQL");
          }
          if (decodeURIComponent(databaseUrl.username) !== process.env.EXPECTED_DATABASE_ROLE) {
            throw new Error("DATABASE_URL does not authenticate as the runtime role");
          }
          if (!databaseUrl.password) {
            throw new Error("DATABASE_URL has no runtime credential");
          }
          if (!databaseUrl.hostname.includes("-pooler.") || !databaseUrl.hostname.endsWith(".neon.tech")) {
            throw new Error("DATABASE_URL is not a pooled Neon connection");
          }
          if (databaseUrl.pathname !== "/" + process.env.EXPECTED_DATABASE_NAME) {
            throw new Error("DATABASE_URL targets the wrong database");
          }
          if (databaseUrl.searchParams.get("sslmode") !== "require") {
            throw new Error("DATABASE_URL must require TLS");
          }
          console.log("Validated seven bounded Preview runtime secrets.");
          NODE
      - name: Verify least-privilege Neon runtime
        run: |
          node --input-type=module <<'NODE'
          import { neon } from "@neondatabase/serverless";
          const sql = neon(process.env.DATABASE_URL);
          const rows = await sql.query(
            "SELECT current_user::text AS current_user, current_database()::text AS current_database, rolcanlogin, rolsuper, rolcreatedb, rolcreaterole, rolinherit, rolreplication, rolbypassrls, rolconnlimit FROM pg_catalog.pg_roles WHERE rolname = current_user",
          );
          if (rows.length !== 1) {
            throw new Error("Runtime role was not visible through the runtime connection");
          }
          const role = rows[0];
          if (
            role.current_user !== process.env.EXPECTED_DATABASE_ROLE ||
            role.current_database !== process.env.EXPECTED_DATABASE_NAME ||
            role.rolcanlogin !== true ||
            role.rolsuper !== false ||
            role.rolcreatedb !== false ||
            role.rolcreaterole !== false ||
            role.rolinherit !== false ||
            role.rolreplication !== false ||
            role.rolbypassrls !== false ||
            Number(role.rolconnlimit) !== 10
          ) {
            throw new Error("Neon runtime role does not match the commissioned least-privilege contract");
          }
          console.log("Verified pooled Preview connectivity and least-privilege Neon role.");
          NODE
      - name: Verify private empty R2 bucket access
        run: |
          node --input-type=module <<'NODE'
          import { ListObjectsV2Command, S3Client } from "@aws-sdk/client-s3";
          const client = new S3Client({
            region: "auto",
            endpoint: "https://" + process.env.CLOUDFLARE_ACCOUNT_ID + ".r2.cloudflarestorage.com",
            credentials: {
              accessKeyId: process.env.DNA_R2_ACCESS_KEY_ID,
              secretAccessKey: process.env.DNA_R2_SECRET_ACCESS_KEY,
            },
          });
          const response = await client.send(
            new ListObjectsV2Command({
              Bucket: process.env.EXPECTED_ACCOUNT_BUCKET,
              MaxKeys: 1,
            }),
          );
          if (Number(response.KeyCount ?? 0) !== 0) {
            throw new Error("Preview R2 bucket is not empty before first owner-approved upload");
          }
          console.log("Verified bucket-scoped R2 credentials and empty Preview storage.");
          NODE
      - name: Verify Cloudflare queue inventory
        run: |
          node --input-type=module <<'NODE'
          const headers = {
            Authorization: "Bearer " + process.env.CLOUDFLARE_API_TOKEN,
            "Content-Type": "application/json",
          };
          const base =
            "https://api.cloudflare.com/client/v4/accounts/" +
            process.env.CLOUDFLARE_ACCOUNT_ID;
          async function read(path) {
            const response = await fetch(base + path, { headers });
            const body = await response.json();
            if (!response.ok || body.success !== true) {
              throw new Error("Cloudflare read failed for " + path);
            }
            return body.result;
          }
          const queue = await read(
            "/queues/" + process.env.EXPECTED_ACTIVATION_QUEUE_ID,
          );
          const dlq = await read(
            "/queues/" + process.env.EXPECTED_ACTIVATION_DLQ_ID,
          );
          if (queue.queue_name !== process.env.EXPECTED_ACTIVATION_QUEUE) {
            throw new Error("Activation queue identity changed");
          }
          if (dlq.queue_name !== process.env.EXPECTED_ACTIVATION_DLQ) {
            throw new Error("Activation DLQ identity changed");
          }
          const consumers = await read(
            "/queues/" + process.env.EXPECTED_ACTIVATION_QUEUE_ID + "/consumers",
          );
          const unexpected = consumers.filter(
            (consumer) =>
              consumer.type !== "worker" ||
              consumer.script_name !== process.env.EXPECTED_WORKER_NAME,
          );
          if (unexpected.length > 0 || consumers.length > 1) {
            throw new Error("Activation queue has an unexpected consumer");
          }
          console.log("Verified immutable Queue/DLQ identities and safe consumer inventory.");
          NODE
      - name: Validate Worker bundle without deployment
        run: npm run worker:dry-run
      - name: Materialize ephemeral Worker secrets
        run: |
          node --input-type=module <<'NODE'
          import { writeFileSync } from "node:fs";
          const names = [
            "AUTHORIZED_CLERK_USER_ID",
            "CLOUDFLARE_ACCOUNT_ID",
            "CLOUDFLARE_API_TOKEN",
            "DATABASE_URL",
            "DNA_DATABASE_OWNER_ID",
            "DNA_R2_ACCESS_KEY_ID",
            "DNA_R2_SECRET_ACCESS_KEY",
          ];
          const secrets = Object.fromEntries(
            names.map((name) => {
              const value = process.env[name];
              if (!value) throw new Error("Missing " + name);
              return [name, value];
            }),
          );
          writeFileSync(process.env.WORKER_SECRETS_FILE, JSON.stringify(secrets), {
            encoding: "utf8",
            mode: 0o600,
          });
          NODE
      - name: Deploy protected queue-only Worker
        run: >
          npx wrangler deploy
          --config wrangler.jsonc
          --strict
          --experimental-provision=false
          --experimental-auto-create=false
          --secrets-file "${WORKER_SECRETS_FILE}"
      - name: Remove ephemeral Worker secrets
        if: always()
        run: rm -f -- "${WORKER_SECRETS_FILE}"
      - name: Verify deployed secret bindings
        run: |
          npx wrangler secret list --config wrangler.jsonc > "${RUNNER_TEMP}/worker-secret-list.json"
          node --input-type=module <<'NODE'
          import { readFileSync } from "node:fs";
          const expected = new Set([
            "AUTHORIZED_CLERK_USER_ID",
            "CLOUDFLARE_ACCOUNT_ID",
            "CLOUDFLARE_API_TOKEN",
            "DATABASE_URL",
            "DNA_DATABASE_OWNER_ID",
            "DNA_R2_ACCESS_KEY_ID",
            "DNA_R2_SECRET_ACCESS_KEY",
          ]);
          const bindings = JSON.parse(
            readFileSync(process.env.RUNNER_TEMP + "/worker-secret-list.json", "utf8"),
          );
          const actual = new Set(bindings.map((binding) => binding.name));
          if (
            actual.size !== expected.size ||
            [...expected].some((name) => !actual.has(name))
          ) {
            throw new Error("Deployed Worker secret bindings do not match the runtime contract");
          }
          console.log("Verified seven deployed Worker secret bindings without reading their values.");
          NODE
      - name: Verify exact queue consumer controls
        run: |
          node --input-type=module <<'NODE'
          const response = await fetch(
            "https://api.cloudflare.com/client/v4/accounts/" +
              process.env.CLOUDFLARE_ACCOUNT_ID +
              "/queues/" +
              process.env.EXPECTED_ACTIVATION_QUEUE_ID +
              "/consumers",
            {
              headers: {
                Authorization: "Bearer " + process.env.CLOUDFLARE_API_TOKEN,
                "Content-Type": "application/json",
              },
            },
          );
          const body = await response.json();
          if (!response.ok || body.success !== true) {
            throw new Error("Could not verify the deployed activation consumer");
          }
          const consumers = body.result.filter(
            (consumer) =>
              consumer.type === "worker" &&
              consumer.script_name === process.env.EXPECTED_WORKER_NAME,
          );
          if (body.result.length !== 1 || consumers.length !== 1) {
            throw new Error("Activation queue does not have exactly one expected Worker consumer");
          }
          const consumer = consumers[0];
          const settings = consumer.settings ?? {};
          const deadLetterQueue =
            consumer.dead_letter_queue ?? consumer.dead_letter_queue_name;
          if (
            deadLetterQueue !== process.env.EXPECTED_ACTIVATION_DLQ ||
            Number(settings.batch_size) !== 1 ||
            Number(settings.max_concurrency) !== 1 ||
            Number(settings.max_retries) !== 3 ||
            Number(settings.max_wait_time_ms) !== 5000
          ) {
            throw new Error("Deployed queue consumer controls differ from the protected contract");
          }
          console.log("Verified queue-only Worker, DLQ, retry, batch, wait, and concurrency controls.");
          NODE
      - name: Record deployed Worker inventory
        run: npx wrangler deployments list --config wrangler.jsonc
