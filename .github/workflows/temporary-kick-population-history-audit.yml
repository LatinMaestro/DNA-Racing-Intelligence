name: TEMPORARY population history audit kick retained for audit trail

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retained-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary audit dispatcher is disabled; do not merge this branch."
