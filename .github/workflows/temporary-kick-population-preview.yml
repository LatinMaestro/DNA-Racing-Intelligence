name: TEMPORARY population Preview kick retained for audit trail

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retained-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary population dispatcher is disabled; do not merge this branch."
