import { ModulePlaceholder } from "@/components/module-placeholder";

export default function VaultPage() {
  return (
    <ModulePlaceholder
      boundary="Phase 2 will resolve owned cores by durable ID, retain manual identity-review cases and expose lineage without treating name matches as authoritative."
      href="/vault"
      source="Vault"
    />
  );
}
