import { ModulePlaceholder } from "@/components/module-placeholder";

export default function OpenRacePage() {
  return (
    <ModulePlaceholder
      boundary="Phase 8 Stage A will select a core without current-race stars. Optional Stage B observations become available only after field lock and can never recommend switching the committed core."
      href="/open-race"
      source="Race"
    />
  );
}
