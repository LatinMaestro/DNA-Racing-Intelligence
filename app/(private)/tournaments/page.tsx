import { ModulePlaceholder } from "@/components/module-placeholder";

export default function TournamentsPage() {
  return (
    <ModulePlaceholder
      boundary="Phase 4 will make qualification rules configurable and treat the 50% gate rule only as a cap. Imported history will never be described as the current qualifying field."
      href="/tournaments"
      source="Race"
    />
  );
}
