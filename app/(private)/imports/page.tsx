import { ModulePlaceholder } from "@/components/module-placeholder";

export default function ImportsPage() {
  return (
    <ModulePlaceholder
      boundary="Phase 1 will add private, idempotent imports, batch provenance, schema validation, rollback and aggregate refresh jobs."
      href="/imports"
      source="Race"
    />
  );
}
